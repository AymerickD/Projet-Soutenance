<div class="card my-2">
    <a href="{{ path('show_artwork', {'artwork_id': artwork.id}) }}"><img src="{{ vich_uploader_asset(artwork, 'imageFile') }}" class="card-img-top" alt="..." style="height: 300px;"></a>
    <div class="card-body">
        <h5 class="card-title">{{ artwork.gallery.user.username }}</h5>
        <p class="card-text">{{ artwork.price | number_format(2, '.', ' ') }} €</p>
        <a href="{{ path('cart_add', {'id': artwork.id}) }}" class="btn btn-success float-right">
            <i class="fas fa-shopping-cart mr-1"></i>
            Ajouter
        </a>
    </div>
</div>