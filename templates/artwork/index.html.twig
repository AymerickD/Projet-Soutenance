{% extends 'base.html.twig' %}

{% block title %}Les produits
{% endblock %}

{% block body %}
<div class="bg-light p-3">
    <h1>Les produits</h1>
</div>

<div class="rowc container">

    {% for artwork in artworks %}
    <div class="col-4 mt-4">
        <div class="border border-dark rounded p-2 clearfix">
            <h2>
                Artwork Name : {{ artworks.title }}
            </h2>
            <img class="img-fluid rounded " src=" {{ artworks.title }} " alt="Image de ">
            <hr>
            <h2> {{ artworks.title }} €</h2>
            <a href="{{ path('cart_add', {'id': artworks.id}) }}" class="btn btn-success float-right">
                <i class="fas fa-shopping-cart mr-1"></i>
                Ajouter
            </a>
        </div>
    </div>
    {% endfor %}
</div>
{% endblock %}